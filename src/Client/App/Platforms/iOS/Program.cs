using UIKit;

namespace Bit.Fluent.Player.Client.App.Platforms.iOS;

public class Program
{
    static void Main(string[] args)
    {
        UIApplication.Main(args, null, typeof(AppDelegate));
    }
}